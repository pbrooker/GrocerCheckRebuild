@model IEnumerable<GrocerCheckRebuild.Models.Grocer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GrocerName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GrocerName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GrocerID }) |
            @Html.ActionLink("Details", "Details", new { id=item.GrocerID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GrocerID })
        </td>
    </tr>
}

</table>
@Html.ActionLink("Return Home", "Index", "Home")
